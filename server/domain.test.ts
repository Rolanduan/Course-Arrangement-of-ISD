import test from 'node:test'; import assert from 'node:assert/strict'; import { validateAllocations } from './domain.js';
test('detects teacher and room conflicts',()=>{ const sections:any=[{id:1,teacherId:1,studentIds:[],name:'A'},{id:2,teacherId:1,studentIds:[],name:'B'}]; const rooms:any=[{id:1,name:'R',capacity:20}]; const teachers:any=[{id:1,name:'T'}]; const allocations:any=[{id:1,sectionId:1,day:1,slot:1,duration:1,roomId:1},{id:2,sectionId:2,day:1,slot:1,duration:1,roomId:1}]; assert.deepEqual(validateAllocations(allocations,sections,rooms,teachers).map(x=>x.type),['TEACHER','ROOM']); });
